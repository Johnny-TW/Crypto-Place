# Docker Compose Environment Variables Example
# Copy this file to .env.dev, .env.qas, or .env.prd and fill in your values

# Node Environment (development, production)
NODE_ENV=development

# Database Configuration
POSTGRES_DB=crypto_place
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_PORT=5433

# Service Ports
FRONTEND_PORT=3001
BACKEND_PORT=5001

# API URLs
# For Docker: use service name (http://backend:5001)
# For local: use localhost or external IP
VITE_API_BASE_URL=http://backend:5001
